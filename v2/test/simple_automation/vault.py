import simple_automation
import os

#def load_vault(file: str):
#    canonical_path = os.path.realpath(file)
#    if canonical_path in simple_automation.loaded_vaults:
#        return simple_automation.loaded_vaults[canonical_path]
#
#    print(f"loading vault {file}")
#    # TODO
#    vault = {}
#    simple_automation.loaded_vaults[canonical_path] = vault
#    return vault
